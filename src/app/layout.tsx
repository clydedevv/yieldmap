import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "🌀 BTC Yield Explorer - Discover Bitcoin DeFi Opportunities",
  description: "Discover and explore Bitcoin yield opportunities across DeFi protocols. Navigate yield strategies, compare rates, and find the best BTC earning opportunities.",
  keywords: ["Bitcoin", "BTC", "yield", "DeFi", "liquidity", "staking", "farming"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
